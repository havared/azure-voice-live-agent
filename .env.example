# =============================================================================
# Azure Voice Live Configuration
# =============================================================================

# Your Azure AI Foundry resource endpoint (Keys & Endpoint section in portal)
AZURE_VOICELIVE_ENDPOINT=https://<your-resource-name>.services.ai.azure.com

# Your Foundry project name
AZURE_VOICELIVE_PROJECT_NAME=<your-project-name>

# Your Foundry Agent ID (created in the Agents playground)
AZURE_VOICELIVE_AGENT_ID=<your-agent-id>

# API version
AZURE_VOICELIVE_API_VERSION=2025-10-01

# API key (Keys & Endpoint section in portal — use KEY1 or KEY2)
AZURE_VOICELIVE_API_KEY=<your-api-key>

# =============================================================================
# Service Principal (for agent access token — no az login required)
# Register an app in Entra ID, assign "Cognitive Services User" role,
# then fill in the values below.
# =============================================================================

AZURE_TENANT_ID=<your-tenant-id>
AZURE_CLIENT_ID=<your-client-id>
AZURE_CLIENT_SECRET=<your-client-secret>

# =============================================================================
# Custom Neural Voice
# =============================================================================

# Your custom neural voice name (e.g. en-US-YourBrandNeural)
AZURE_VOICELIVE_VOICE_NAME=<your-custom-voice-name>

# Custom voice deployment endpoint ID (a GUID from the Custom Voice portal)
AZURE_VOICELIVE_VOICE_ENDPOINT_ID=<your-custom-voice-endpoint-id>

# =============================================================================
# Voice Activity Detection (VAD) Configuration
# =============================================================================

VAD_THRESHOLD=0.5
VAD_PREFIX_PADDING_MS=300
VAD_SILENCE_DURATION_MS=500

# =============================================================================
# Application Settings
# =============================================================================

APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO

# If true, the agent speaks first with a greeting when session starts
ENABLE_PROACTIVE_GREETING=true
